Install the following mmodules-
  1.bs4
  2.selenium

then run the command python "filename.py" and you will see the data will scrapped in terminal and the scrapped data will also be stored in a file named crawled_data.txt
